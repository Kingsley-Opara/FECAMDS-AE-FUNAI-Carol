scripts:
  server:
  - cd src && python manage.py runserver
  makemigrations:
  - cd src && python manage.py makemigrations
  migrate:
  - cd src && python manage.py migrate
  gunicorn:
  - cd src && uvicorn core.asgi:application --host 0.0.0.0 --port 8001 --reload
